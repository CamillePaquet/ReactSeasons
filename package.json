{
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "build": "rimraf dist && parcel build --reporter @parcel/reporter-bundle-analyzer",
    "format": "prettier --write srcyarn ",
    "prepare": "husky install",
    "commit": "git-cz"
  },
  "devDependencies": {
    "@parcel/reporter-bundle-analyzer": "^2.3.0",
    "husky": ">=6",
    "lint-staged": ">=10",
    "parcel": "^2.3.0",
    "prettier": "^2.5.1",
    "rimraf": "^3.0.2"
  },
  "lint-staged": {
    "{*,src/**/*}.{js,css,md,json,html}": "prettier --write"
  }
}
