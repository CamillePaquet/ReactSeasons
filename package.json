{
    "dependencies": {
        "react": "^17.0.2",
        "react-dom": "^17.0.2"
    },
    "source": "src/index.html",
    "scripts": {
        "start": "parcel",
        "build": "rimraf dist && parcel build"
    },
    "devDependencies": {
        "@parcel/reporter-bundle-analyzer": "^2.3.0",
        "parcel": "^2.3.0",
        "rimraf": "^3.0.2"
    }
}
